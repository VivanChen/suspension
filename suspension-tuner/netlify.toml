[build]
  command = "npm run build"
  publish = "dist"

# 讓瀏覽器一定向網路要新版 SW（避免舊 SW 把自己快取死）
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-store"

# SPA fallback：Netlify 仍會先對 publish 內實體檔案短路；這裡只接住「沒有對應檔」的路徑
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
